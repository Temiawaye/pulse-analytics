import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function SourcesPage() {
  return (
    <PagePlaceholder
      description="See which direct, search, social, and referral sources drive visits."
      eyebrow="Acquisition"
      title="Sources"
    />
  );
}
