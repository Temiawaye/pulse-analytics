import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function OverviewPage() {
  return (
    <PagePlaceholder
      description="Monitor traffic, audience quality, top content, and recent visits at a glance."
      eyebrow="Dashboard"
      title="Overview"
    />
  );
}
