import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function PagesPage() {
  return (
    <PagePlaceholder
      description="Compare page views and unique visitors across every tracked path."
      eyebrow="Content"
      title="Pages"
    />
  );
}
